<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All follows
    </h1>

    <a href="/follows/new" class="btn btn-block btn-outline-secondary">
      Add a new follow
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Stock
        </th>

        <th>
          Notes
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @follows.each do |follow| %>
      <tr>
        <!-- Display follow.id start -->
        <td>
          <%= follow.id %>
        </td>
        <!-- Display follow.id end -->

        <!-- Display follow.user_id start -->
        <td>
          <% if follow.user.present? %>
            <a href="/users/<%= follow.user.id %>">
              <%= follow.user.username %>
            </a>
          <% end %>
        </td>
        <!-- Display follow.user_id end -->

        <!-- Display follow.stock_id start -->
        <td>
          <% if follow.stock.present? %>
            <a href="/stocks/<%= follow.stock.id %>">
              <%= follow.stock.ticker %>
            </a>
          <% end %>
        </td>
        <!-- Display follow.stock_id end -->

        <!-- Display follow.notes start -->
        <td>
          <%= follow.notes %>
        </td>
        <!-- Display follow.notes end -->

        <!-- Display follow.created_at start -->
        <td>
          <%= time_ago_in_words(follow.created_at) %> ago
        </td>
        <!-- Display follow.created_at end -->

        <!-- Display follow.updated_at start -->
        <td>
          <%= time_ago_in_words(follow.updated_at) %> ago
        </td>
        <!-- Display follow.updated_at end -->

        <td>
          <a href="/follows/<%= follow.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @follows, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
